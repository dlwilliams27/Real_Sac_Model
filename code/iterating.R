
#path for all forcing data
files <- list.files(path='Forcings', pattern = '\\.csv$')
file_list=list(files)
#loop to execute modeling for forcing data
for (file in file_list) {}












write.table()